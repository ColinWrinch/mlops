name: deploy-model-training-pipeline

on: 
  workflow_dispatch:
jobs:
  get-config:
    uses: Azure/mlops-templates/.github/workflows/read-yaml.yml@main
    with:
      file_name: config-infra-prod.yml

  create-compute:
    needs: get-config
    uses: Azure/mlops-templates/.github/workflows/create-compute.yml@main
    with:
      cluster_name: cpu-cluster
      size: Standard_DS11_v2
      min_instances: 0
      max_instances: 1
      cluster_tier: dedicated
      resource_group: ${{ needs.get-config.outputs.resource_group }}
      workspace_name: ${{ needs.get-config.outputs.aml_workspace }}     
    secrets:
      creds: ${{secrets.AZURE_CREDENTIALS}}
        
  register-dataset:
    needs: get-config
    uses: Azure/mlops-templates/.github/workflows/register-dataset.yml@main
    with:
      resource_group: ${{ needs.get-config.outputs.resource_group }}
      workspace_name: ${{ needs.get-config.outputs.aml_workspace }}
      name: used-cars-data
      data_file: mlops/azureml/train/data.yml
    secrets:
      creds: ${{secrets.AZURE_CREDENTIALS}}
  
  register-environment:
    needs: get-config
    uses: Azure/mlops-templates/.github/workflows/register-environment.yml@main
    with:
      resource_group: ${{ needs.get-config.outputs.resource_group }}
      workspace_name: ${{ needs.get-config.outputs.aml_workspace }}
      environment_file:  mlops/azureml/train/train-env.yml
      conda_file: data-science/environment/train-conda.yml
    secrets:
      creds: ${{secrets.AZURE_CREDENTIALS}}


  run-pipeline:
    needs: [get-config, register-environment, create-compute, register-dataset]
    uses: Azure/mlops-templates/.github/workflows/run-pipeline.yml@main
    with:
      resource_group: ${{ needs.get-config.outputs.resource_group }}
      workspace_name: ${{ needs.get-config.outputs.aml_workspace }}
      parameters-file:  mlops/azureml/train/newpipeline.yml
      job-name: mlops-pipeline
    secrets:
      creds: ${{secrets.AZURE_CREDENTIALS}}

  # run-pipeline:
  #   needs: [get-config, register-environment, create-compute, register-dataset]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Show the inputs to the run-pipeline job
  #       run: |
  #         echo "Resource Group: ${{ needs.get-config.outputs.resource_group }}"
  #         echo "AML Workspace: ${{ needs.get-config.outputs.aml_workspace }}"
  #         echo "Parameter file: mlops/azureml/train/newpipeline.yml"
  #         echo "Job name: mlops-pipeline"
      
  #     - name: Check if Compute Target is ready
  #       run: |
  #         az ml compute show --name cpu-cluster --resource-group ${{ needs.get-config.outputs.resource_group }} --workspace-name ${{ needs.get-config.outputs.aml_workspace }} --verbose
      
  #     - name: Check if Dataset is registered
  #       run: |
  #         az ml data list --resource-group ${{ needs.get-config.outputs.resource_group }} --workspace-name ${{ needs.get-config.outputs.aml_workspace }} --verbose
      
  #     - name: Check if Environment is registered
  #       run: |
  #         az ml environment list --resource-group ${{ needs.get-config.outputs.resource_group }} --workspace-name ${{ needs.get-config.outputs.aml_workspace }} --verbose
      
  #     - name: Run the pipeline and capture detailed logs
  #       run: |
  #         echo "Starting pipeline execution..."
  #         az ml pipeline run submit --experiment-name mlops-pipeline --file mlops/azureml/train/newpipeline.yml --resource-group ${{ needs.get-config.outputs.resource_group }} --workspace-name ${{ needs.get-config.outputs.aml_workspace }} --verbose
      
  #     - name: Run the Pipeline Using the Template
  #       uses: Azure/mlops-templates/.github/workflows/run-pipeline.yml@main
  #       with:
  #         resource_group: ${{ needs.get-config.outputs.resource_group }}
  #         workspace_name: ${{ needs.get-config.outputs.aml_workspace }}
  #         parameters-file: mlops/azureml/train/newpipeline.yml
  #         job-name: mlops-pipeline
  #       secrets:
  #         creds: ${{secrets.AZURE_CREDENTIALS}}



# run-pipeline:
#   needs: [get-config, register-environment, create-compute, register-dataset]
#   runs-on: ubuntu-latest
#   steps:
#     - name: Check if Compute Target is ready
#       run: |
#         az ml compute show --name cpu-cluster --resource-group ${{ needs.get-config.outputs.resource_group }} --workspace-name ${{ needs.get-config.outputs.aml_workspace }} || { echo "Compute target not found!"; exit 1; }

#     - name: Check if Dataset is registered
#       run: |
#         az ml data list --resource-group ${{ needs.get-config.outputs.resource_group }} --workspace-name ${{ needs.get-config.outputs.aml_workspace }} || { echo "Dataset not found!"; exit 1; }

#     - name: Check if Environment is registered
#       run: |
#         az ml environment list --resource-group ${{ needs.get-config.outputs.resource_group }} --workspace-name ${{ needs.get-config.outputs.aml_workspace }} || { echo "Environment not found!"; exit 1; }

#     - name: Run the Pipeline Using the Template
#       uses: Azure/mlops-templates/.github/workflows/run-pipeline.yml@main
#       with:
#         resource_group: ${{ needs.get-config.outputs.resource_group }}
#         workspace_name: ${{ needs.get-config.outputs.aml_workspace }}
#         parameters-file: mlops/azureml/train/newpipeline.yml
#         job-name: mlops-pipeline
#       secrets:
#         creds: ${{secrets.AZURE_CREDENTIALS}}
      
  # Original
  # run-pipeline:
  #   needs: [get-config, register-environment, create-compute, register-dataset]
  #   uses: Azure/mlops-templates/.github/workflows/run-pipeline.yml@main
  #   with:
  #     resource_group: ${{ needs.get-config.outputs.resource_group }}
  #     workspace_name: ${{ needs.get-config.outputs.aml_workspace }}
  #     parameters-file:  mlops/azureml/train/newpipeline.yml
  #     job-name: mlops-pipeline
  #   secrets:
  #     creds: ${{secrets.AZURE_CREDENTIALS}}
